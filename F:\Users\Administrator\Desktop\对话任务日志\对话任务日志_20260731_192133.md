
## 第四轮：Agent常驻保活（20:10）

### 主人指令
调用鲁班/墨工完事后不需要马上关掉，冷启动时间太长。放在那边，后续调用直接发命令。物理层Agent不需要关闭。

### 根因分析
1. **dispatch_to_luban.py**：每次调用创建新 ClaudeStreamJsonClient → 新 claude 进程 → 用完 close() 杀掉 → 下次又冷启动(30-40s)
2. **Hermes delegate_task**：每次创建新 AIAgent → 新 ClaudeStreamJsonClient 实例 → delegate 完成后 child.close() → 进程被杀 → 下次又冷启动
3. **Codex 同理**：每次 codex exec 新进程 → 48秒冷启动

### 修复
**鲁班(Claude Code) — claude_client.py：**
1. close() 不杀 _persistent_proc，不设 is_closed=True
2. 加模块级 _shared_persistent_proc + _shared_persistent_lock + _shared_last_active + _shared_call_count
3. _ensure_persistent_process 用全局共享进程（所有实例复用同一 claude 进程）
4. 加 get_shared_client() 单例函数
5. dispatch_to_luban.py 改用 get_shared_client()，不再每次 close

**墨工(Codex) — codex_client.py：**
1. 同样的模式：close() 不杀持久进程
2. 模块级共享进程 + 单例 get_shared_client()
3. _ensure_persistent_process 用全局共享进程

### 效果
- 第1次调用：30-48s（冷启动，无法避免）
- 后续调用：5-7s（热启动，复用持久进程）
- close() 被调用后进程不死，等下次 _ensure_persistent_process 直接复用
- 空闲 600s 后自动回收（_idle_timeout）

### 改动文件
- claude_client.py（鲁班）：close + 单例 + 全局共享进程
- codex_client.py（墨工）：同上
- dispatch_to_luban.py：改用 get_shared_client() 单例
